export function getAllPostsFromSubreddit(
  subredditID: string,
  sinceWhen: string
) {
  // hit Reddit API to get posts since a particular time
}

export function getSubredditInfo(subredditName: string) {
  // might be needed - given a name, get the subreddit info
  // good opportunity for a cache
}
